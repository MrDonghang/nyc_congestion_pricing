type: deepar
context_length: 180
num_layers: 2
dropout_rate: 0.1
max_epochs: 120
num_samples: 100
coverage_level: 0.9
optuna:
  n_trials: 30
  search_space:
    context_length: { type: int, low: 60, high: 400 }
    num_layers: { type: int, low: 2, high: 5 }
    dropout_rate: { type: float, low: 0.0, high: 0.5 }
